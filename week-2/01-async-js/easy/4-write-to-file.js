const fs = require("fs");

const pathToFile = "writeFileDemo.txt";

fs.writeFile(
  pathToFile,
  "This is some random text generated by 4-write-to-file.js",
  (err, data) => {
    if (err) {
      console.log(err);
    }
  }
);
